# 缺陷修复记录

## 边界与证据

本轮于2026-01-16执行。只修改独立修复副本；旧版业务代码、原数据库及首轮记录不覆盖。旧测试入口5001继续对应旧版；本轮使用5011，运行结束已停止。没有将本次修复部署到原项目。

## 图书系统

|编号|根因与修改位置|修复方案|影响范围与回归|结论|
|---|---|---|---|---|
|BUG-001|app.py新增/编辑库存直接int转换|统一校验缺失、非整数、负数、32位范围，非法返回400|新增、编辑库存；abc、空串、小数、负数、越界以及合法值|修复通过|
|BUG-002|app.py借阅编号直接int(None)|提交前校验缺失、格式及正数范围|空表单、缺单参数、非法编号、正常借阅|修复通过|
|BUG-003|services.py负库存静默归零，首轮为需求待确认|本轮测试副本采用拒绝负库存规则；路由和服务层校验一致|BOOK-05通过；该规则是本轮明确的测试副本决策，不倒写为原有已确认需求|规则落实并通过|
|BUG-004|scoped_session未在请求结束移除，连接池累积占用|app.py teardown_request逐库remove；异常事务由remove回滚|34项HTTP连续执行，另加同一服务90次列表请求；未重启规避|修复通过，非长时压力认证|
|BUG-005|删除/归还路由忽略业务返回消息|flash反馈并在主模板转义显示|在借图书/读者删除拒绝、重复归还、不存在借阅|修复通过|
|BUG-006|先按is_overdue标志筛选导致实际逾期漏查|services.py按借阅日期阈值及未归还状态查询，不通过查询修改数据|31天/29天/已归还，三库核对|修复通过|
|BUG-007|读库存后在内存减1，并发丢失更新|services.py条件UPDATE原子扣库存，检查影响行数；重复借阅查询与新增置于同一事务，失败回滚|原HTTP并发场景；三库各3轮库存1并发；同读者并发重复借阅|修复通过|

### 关联风险加固

- 并发重复归还：通过条件UPDATE仅认领未归还记录，原子加库存；三库各1轮双线程归还验证。
- 请求身份隔离：业务权限从ContextVar读取，app写入请求上下文，结束清理。保留旧current_user变量仅兼容顺序服务测试；权限判断不再读该共享变量。管理员查询与查看员写入并发12轮全部符合预期。
- 新增/编辑、借阅与归还事务属于本轮影响范围；同步和报表跨库交互未验证。

## 外卖平台：限定两类闭环

|编号|根因与位置|方案|验证|结论|
|---|---|---|---|---|
|TAKE-004|databaseinterface.cpp拼接SQL，单引号导致插入失败；commmsgmanager.cpp先删后插未保证完整事务|用户、菜品、订单插入采用参数绑定；删除采用绑定；菜单/订单删除与插入同事务，失败回滚|单引号用户/菜品/订单；数据库测试触发器强制插入失败；旧记录完整保留；故障后恢复|修复通过|
|TAKE-007|QString默认浮点转字符串丢失有效位|以double参数绑定写入SQLite，不经默认精度字符串转换|订单及菜品123456.78、9999.99、0.01存储核对|修复通过|

第一次修复后54项执行为37通过、17失败。强制插入失败的两项暴露事务仍被get列表方法中的execSql提前提交；第二次把事务起点移到读取旧数据之后，再次完整执行54项，39通过、15失败。两次记录分别保留，没有覆盖。

其余8类问题：状态流转、负金额、重复标识、未认证写入、注册校验、手机号截断、明文密码返回、必填字段校验，仍待处理。
详见修复代码差异.patch、原始JSON快照和服务日志。
